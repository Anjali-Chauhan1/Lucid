# Offline training pipeline for Lucid. NEVER deployed — the app runs inference
# in TypeScript via transformers.js. This exists to produce lib/ml/weights.json
# and the metrics artifacts committed to the repo.
sentence-transformers>=3.0.0
transformers>=4.44.0
torch>=2.2.0
scikit-learn>=1.5.0
pandas>=2.2.0
numpy>=1.26.0
matplotlib>=3.8.0
tqdm>=4.66.0

# Only needed for generate_data.py in --online mode (LLM-authored samples)
anthropic>=0.40.0

# Only needed for finetune_embeddings.py --export-onnx (Layer 1)
optimum[exporters]>=1.20.0
onnx>=1.16.0
